#@meta {desc: 'training (only) configuration', date: '2024-02-01'}

[import]
sections = list: lp_imp

[lp_imp]
config_file = etc/lp.conf

[amr_trainer_default]
trainer_type = parse_xfm
model_name = lp_parse_${amr_default:parse_model}

[amr_default]
parse_model = xfm_bart_base

[amr_xfm_trainer]
# both of the following need to be uncommented if training from scratch
#pretrained_path_or_model = facebook/bart-base
#training_config_file = resource(zensols.amr): resources/train/model_parse_xfm_bart_base.json
training_config_overrides = dict: {
  'hf_args': {'fp16': False,
              'num_train_epochs': 1}}
#training_config_overrides = dict: {'hf_args': {'fp16': False}}
